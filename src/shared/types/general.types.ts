export enum StatusCodesEnum {
  ok = 200,
  unauthorized = 401,
  forbidden = 403,
  badRequest = 400,
}
export enum EnumTokens {
	'ACCESS_TOKEN' = 'accessToken',
	'REFRESH_TOKEN' = 'refreshToken'
}